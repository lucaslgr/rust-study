//A struct that doesn't have any field is called as a unit struct
struct LookMaNoFields{}

//A struct using Generics type
struct Pair<T> {x: T, y: T};

//Struct defining a type name for a Tuple
struct Color(u8, u8, u8);